# Permission codes for the trip_expenses module, per ARCHITECTURE.md §9.2.
# Seeded (and granted to roles) in migration f27a4c6e9b13, following the
# same pattern as trip_catches (d96d76e5af7a).
TRIP_EXPENSE_VIEW = "trip_expense:view"
TRIP_EXPENSE_CREATE = "trip_expense:create"
TRIP_EXPENSE_EDIT = "trip_expense:edit"
TRIP_EXPENSE_DELETE = "trip_expense:delete"
