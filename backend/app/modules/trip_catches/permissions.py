# Permission codes for the trip_catches module, per ARCHITECTURE.md §9.2.
# Seeded (and granted to roles) in migration d96d76e5af7a, following the
# same pattern as boats (72d5f6096c81).
TRIP_CATCH_VIEW = "trip_catch:view"
TRIP_CATCH_CREATE = "trip_catch:create"
TRIP_CATCH_EDIT = "trip_catch:edit"
TRIP_CATCH_DELETE = "trip_catch:delete"
